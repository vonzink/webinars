version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Static site - no build step"
  artifacts:
    baseDirectory: .
    files:
      - index.html
      - webinars/**/*
  cache:
    paths: []
